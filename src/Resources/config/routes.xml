<?xml version="1.0" encoding="UTF-8" ?>
<routes xmlns="http://symfony.com/schema/routing"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://symfony.com/schema/routing
        http://symfony.com/schema/routing/routing-1.0.xsd">
    <route id="moukail_password_reset.request" controller="moukail_password_reset.password_reset_controller:request" path="/request/reset/password" methods="POST">
        <!-- settings -->
    </route>
    <route id="moukail_password_reset.reset" controller="moukail_password_reset.password_reset_controller:reset" path="/reset/password/{token}" methods="POST">
        <!-- settings -->
    </route>
</routes>
